# Configuración Nginx para Costos Embutidos - Google Cloud Run
# En Cloud Run, el frontend es un servicio independiente sin proxy al backend
worker_processes auto;
worker_rlimit_nofile 1024;

events {
    worker_connections 512;
    multi_accept on;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Optimizaciones de rendimiento
    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout  30;
    types_hash_max_size 2048;

    # Buffer sizes
    client_body_buffer_size 10K;
    client_header_buffer_size 1k;
    client_max_body_size 8m;
    large_client_header_buffers 2 1k;

    # Compresión gzip para mejorar velocidad
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;

    # Logs a stdout/stderr para Cloud Run
    access_log /dev/stdout combined;
    error_log /dev/stderr warn;

    server {
        # Cloud Run inyecta la variable PORT, usamos 80 por defecto
        listen 80;
        server_name localhost _;

        # Headers de seguridad
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # Localización de los archivos del frontend
        # Assets con hash en el nombre (generados por Vite) - cache agresivo
        location /assets/ {
            root   /usr/share/nginx/html;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Archivos estáticos sin hash (favicon, etc.)
        location ~* \.(png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
            root   /usr/share/nginx/html;
            expires 7d;
            add_header Cache-Control "public";
        }

        # index.html NUNCA debe cachearse - regla explícita
        # para que el navegador siempre obtenga la versión más reciente
        # con las referencias correctas a los chunks de JS/CSS
        location = /index.html {
            root   /usr/share/nginx/html;
            add_header Cache-Control "no-cache, no-store, must-revalidate" always;
            add_header Pragma "no-cache" always;
            add_header Expires "0" always;
        }

        # SPA fallback - todas las rutas caen a index.html
        location / {
            root   /usr/share/nginx/html;
            index  index.html index.htm;
            try_files $uri $uri/ /index.html;
            add_header Cache-Control "no-cache, no-store, must-revalidate" always;
            add_header Pragma "no-cache" always;
            add_header Expires "0" always;
        }

        # Health check endpoint para Cloud Run
        location /health {
            access_log off;
            return 200 'healthy';
            add_header Content-Type text/plain;
        }

        # Manejo de errores
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }
    }
}
