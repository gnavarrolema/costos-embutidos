# ================================================
# VARIABLES DE ENTORNO - Costos Embutidos
# ================================================
# Copie este archivo a .env y configure los valores para su entorno
# NUNCA suba el archivo .env a Git (ya está en .gitignore)

# ===== SEGURIDAD (OBLIGATORIO EN PRODUCCIÓN) =====

# Clave secreta para firmar tokens JWT
# CRÍTICO: Genere una clave segura única para producción
# Ejemplo: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-CHANGE-THIS

# Tiempo de expiración de tokens JWT (en horas)
JWT_EXPIRATION_HOURS=24

# ===== ENTORNO =====

# Entorno de ejecución: development | production
FLASK_ENV=development

# ===== CORS (Solo para producción) =====

# Dominios permitidos para CORS (separados por comas)
# Ejemplo para production: https://app.tudominio.com,https://www.tudominio.com
# En development, CORS está abierto por defecto
ALLOWED_ORIGINS=https://costos-embutidos.pages.dev

# ===== BASE DE DATOS =====

# URI de conexión a la base de datos
# SQLite (por defecto): sqlite:///path/to/costos_embutidos.db
# PostgreSQL ejemplo: postgresql://user:password@host:port/dbname
COSTOS_EMBUTIDOS_DATABASE_URI=sqlite:///backend/costos_embutidos.db

# ===== LOGGING =====

# Nivel de logging: DEBUG, INFO, WARNING, ERROR, CRITICAL
COSTOS_LOG_LEVEL=INFO

# Archivo de logs (opcional, default: backend/logs/app.log)
# COSTOS_LOG_FILE=/var/log/costos-embutidos/app.log

# Tamaño máximo del archivo de log en bytes (default: 5MB)
# COSTOS_LOG_MAX_BYTES=5242880

# Cantidad de archivos de log a rotar (default: 5)
# COSTOS_LOG_BACKUP_COUNT=5

# ===== OTROS =====

# Versión de la aplicación (opcional)
# COSTOS_APP_VERSION=1.5.0

# Saltar inicialización de DB (útil para tests)
# COSTOS_EMBUTIDOS_SKIP_INIT_DB=0
